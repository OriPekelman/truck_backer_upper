{
  "name": "truck_backer_upper",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "watch": "mkdir -p bin/weights bin/bundles && cp screenshot.png ./bin && cp -r src/weights/* bin/weights/ && cp -r src/bundles/* bin/bundles/ && cp src/**/*.svg ./bin/ && webpack --watch",
    "build": "mkdir -p bin/weights bin/bundles && cp screenshot.png ./bin && cp -r src/weights/* bin/weights/ && cp -r src/bundles/* bin/bundles/ && cp src/**/*.svg ./bin/ && webpack ",
    "build-prod": "(export NODE_ENV=production; mkdir -p bin/weights bin/bundles && cp screenshot.png ./bin && cp -r src/weights/* bin/weights/ && cp -r src/bundles/* bin/bundles/ && cp src/**/*.svg ./bin/ && webpack)",
    "tbu": "tsc -p tsconfig.cli.json && node cli-build/cli/tbu.js",
    "check": "tsc -p tsconfig.cli.json && node scripts/check.js"
  },
  "author": "Tino Fuhrmann",
  "license": "GPL-3.0",
  "devDependencies": {
    "@types/node": "^8.10.17",
    "copy-webpack-plugin": "^4.0.1",
    "css-loader": "^0.28.4",
    "style-loader": "^0.18.2",
    "ts-loader": "^2.3.2",
    "typescript": "^2.9.1",
    "uglifyjs-webpack-plugin": "^1.2.7",
    "webpack": "^3.10.0"
  },
  "dependencies": {
    "@types/bootstrap": "^3.3.39",
    "@types/highcharts": "^5.0.4",
    "@types/jquery": "^3.2.11",
    "@types/rc-slider": "^8.2.3",
    "@types/react": "^16.3.16",
    "@types/react-dom": "^16.0.5",
    "ajv": "^6.5.0",
    "bootstrap": "^3.3.7",
    "highcharts": "^5.0.14",
    "jquery": "^3.3.1",
    "konva": "^2.1.3",
    "rc-slider": "^8.6.1",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-highcharts": "^16.0.2",
    "react-konva": "^1.1.4"
  }
}
